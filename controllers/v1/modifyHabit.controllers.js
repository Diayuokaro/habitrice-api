import Habit from '#models/habit.models.js'

export default async (req, res) => {
  // if (!req.params.id) return

  // const user = await User.modifyById(req.params.id, req.body)
  // await res.json(user)
}
